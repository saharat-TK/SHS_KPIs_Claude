/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo
.DS_Store
.env*.local

# Local-only Claude settings (keep .claude/launch.json shared)
.claude/settings.local.json

# Local data files
*.xlsx
~$*

# Real personal data (names/emails) — not for version control
schema/Faculty_list.csv

# Google OAuth client secrets — the values belong in .env.local, not on disk
# as a downloaded JSON. See .env.example for the AUTH_* names.
client_secret_*.json
